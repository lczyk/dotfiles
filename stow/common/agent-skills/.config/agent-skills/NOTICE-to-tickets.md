vendored from https://github.com/mattpocock/skills (MIT). see `LICENSE-to-tickets`.

vendored files:
- `skills/to-tickets/` -- plan/spec -> tracer-bullet tickets skill

local modifications:
- ascii-normalised punctuation (em/en-dash -> `--`/`-`, arrow -> `->`, ellipsis -> `...`) -- no content change, repo convention requires ascii-only prose in committed files
- made standalone -- dropped refs to sibling skills `/setup-matt-pocock-skills` (tracker/label config now inferred from the repo or asked) and `/implement` (frontier work described generically)
